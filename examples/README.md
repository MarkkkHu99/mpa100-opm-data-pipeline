# Generated examples

Example reports are not committed because the available outputs were derived
from a laboratory file whose public release permission has not been established.

After placing an authorised input in `data-private/`, generate local examples with:

```bash
python process_once.py data-private/run.opm --output examples/generated
```

The generated directory is intended for local review. Check its data-release
status before committing any contents.

