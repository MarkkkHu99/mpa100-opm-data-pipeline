[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["data-private", "runtime", "output", "outputs"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

